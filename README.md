# practice-app


コンテナ起動
```
docker-compose up -d
```

イメージ一覧
```
docker-compose images
```

コンテナ一覧
```
docker-compose ps -a 
```

コンテナ・イメージ削除
```
docker-compose down --rmi all --volumes --remove-orphans
```
